#pragma once
#include <windows.h>

void UpdateAimbot(bool all, HANDLE hProcess, uintptr_t moduleBase, uintptr_t localPlayer, bool enabled, float fov, float maxDistance, float headoffset, int legit_aim_procent, int aimbot_aimat);
